# RISCV-based-MYTH- by VSD
1.GitHub Codespace setup: riscv codespace link provided with intro mail opened a codespace. Followed " Code--> Codespaces-->Create a codespace on the main


2.Verified setupo and Sample program run in codespace

3.Linux setup done in cloud a. Clicked on Follow forwarded address link of noVNC Desktop Port under PORTS Tab in the codespace.


4.New window.

5.Opened a terminal in the linux screen appeared in browser tab, navigated to samples folder using the following commands cd /workspaces/vsd-riscv2 cd samples ls -ltr and an sample program to find sum and saw output using following commands (using Native GCC (x86)) gcc sum1ton.c . /a.out

6.Opened sum1ton.c on "gedit" gedit sum1ton.c &

7.Make changes n=15 in the gedit (GUI Editor),save the file, and re-run. Thus we get desired output within terminal.


